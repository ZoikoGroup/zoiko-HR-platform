# Dev-only tooling for email assets and previews (not needed at runtime).
#   pip install -r scripts/requirements-email-tools.txt
#   python -m playwright install chromium
resvg-py>=0.5.0
playwright>=1.45.0
